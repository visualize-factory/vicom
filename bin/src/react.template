import React, { Component } from 'react';
import ReactDom from 'react-dom';
import <%= name %> from '<%= comRelative %>';

ReactDom.render(
  <<%= name %> />,
  document.getElementById('container')
);
